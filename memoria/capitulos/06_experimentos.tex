\chapter{Experimentos}

\section{Protocolo experimental}

La Fase 2 introduce un protocolo experimental local para pasar de una unica
ejecucion valida a un conjunto pequeno de ejecuciones comparables. El protocolo
se implementa en:

\begin{center}
\begin{minipage}{0.85\textwidth}
\begin{verbatim}
codigo/app/services/experiment_protocol.py
\end{verbatim}
\end{minipage}
\end{center}

La unidad principal es un manifiesto `ExperimentPlan`, compuesto por varias
entradas `ExperimentSpec`. Cada entrada fija un `run\_id`, una descripcion y una
configuracion de modelado validada mediante `ModelingConfig`. La ejecucion del
plan reutiliza el wrapper persistido del pipeline y, al finalizar, invoca el
registro consultable para comparar metricas entre runs.

Cada plan conserva tres artefactos agregados:

\begin{itemize}
  \item `experiment\_plan.json`, con el manifiesto experimental;
  \item `comparison.json`, con la comparacion estructurada;
  \item `results\_table.md`, con la tabla resumida para documentacion.
\end{itemize}

Ademas, los modelos, predicciones, metricas e informes finales se escriben
dentro de la carpeta de cada experimento para evitar sobrescrituras entre
runs comparables.

\section{Plan CWRU Isolation Forest Threshold}

El primer plan experimental real se identifica como:

\begin{center}
\begin{minipage}{0.85\textwidth}
\begin{verbatim}
cwru_iforest_threshold_v1
\end{verbatim}
\end{minipage}
\end{center}

El objetivo es estudiar una variacion controlada del umbral de decision del
modelo base Isolation Forest. No se introduce ningun algoritmo nuevo, ya que la
regla de la arquitectura exige que todo modelo disponible para un agente tenga
un ejecutor determinista reproducible.

Los dos runs ejecutados han sido:

\begin{itemize}
  \item `cwru\_iforest\_threshold\_v1\_baseline\_threshold\_099`;
  \item `cwru\_iforest\_threshold\_v1\_conservative\_threshold\_100`.
\end{itemize}

Ambos mantienen constante el dataset, la limpieza, la estructuracion temporal,
el algoritmo, la semilla y `n\_estimators=200`. La unica variable modificada es
`threshold\_quantile`: 0.99 para el baseline y 1.00 para la variante
conservadora.

\begin{table}[h]
\centering
\caption{Comparacion local de umbral con Isolation Forest sobre CWRU}
\label{tab:cwru-iforest-threshold}
\begin{tabular}{lccccc}
\toprule
Experimento & Umbral & Precision & Recall & F1 & FPR \\
\midrule
Baseline & 0.99 & 0.9991 & 1.0000 & 0.9996 & 0.0513 \\
Conservador & 1.00 & 0.9996 & 1.0000 & 0.9998 & 0.0256 \\
\bottomrule
\end{tabular}
\end{table}

Los dos runs quedan aprobados por el agente evaluador. La variante conservadora
reduce la tasa de falsos positivos de 0.0513 a 0.0256 y mejora ligeramente la
precision y el F1-score, manteniendo recall 1.0000. En este benchmark, por
tanto, el umbral conservador resulta preferible para reducir falsas alarmas sin
penalizar la deteccion de fallos.

Los artefactos agregados del plan quedan en:

\begin{center}
\begin{minipage}{0.85\textwidth}
\begin{verbatim}
codigo/experiments/cwru_local/cwru_iforest_threshold_v1/
\end{verbatim}
\end{minipage}
\end{center}

\section{Comparacion Agentica de Ventanas CWRU}

La Fase 3 incorpora un experimento donde el agente estructurador no solo elige
una configuracion, sino que propone alternativas comparables. La ejecucion se
lanzo con:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_cwru_agentic_window_comparison \
  --model qwen3.5:4b \
  --plan-id cwru-agentic-window-qwen-fase3
\end{verbatim}
\end{minipage}
\end{center}

Qwen propuso tres configuraciones de ventana, todas con solape del 50\%:
2048 muestras como seleccion principal, 1024 muestras para aumentar resolucion
temporal y 4096 muestras para aumentar contexto por ventana. El protocolo
experimental materializo esas propuestas sin anadir configuraciones no
decididas por el agente.

\begin{table}[h]
\centering
\caption{Comparacion agentica de ventanas sobre CWRU}
\label{tab:cwru-agentic-window}
\begin{tabular}{lccccc}
\toprule
Ventana & Solape & Precision & Recall & F1 & FPR \\
\midrule
2048 & 0.50 & 0.9991 & 1.0000 & 0.9996 & 0.0513 \\
1024 & 0.50 & 0.9996 & 0.9999 & 0.9998 & 0.0212 \\
4096 & 0.50 & 0.9991 & 1.0000 & 0.9996 & 0.0517 \\
\bottomrule
\end{tabular}
\end{table}

La configuracion de 1024 muestras obtuvo la mejor precision, el mejor F1-score
y la menor tasa de falsos positivos. La configuracion principal de 2048
muestras mantuvo recall 1.0000, pero produjo mas falsas alarmas. Este resultado
apoya la idea central del sistema: los agentes formulan hipotesis tecnicas y la
aplicacion las convierte en evidencia experimental trazable.

Los artefactos quedan en:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
codigo/experiments/cwru_local/cwru-agentic-window-qwen-fase3/
\end{verbatim}
\end{minipage}
\end{center}

\section{Comparacion Agentica de Modelos CWRU}

Tras seleccionar la ventana de 1024 muestras, se ejecuto una comparacion
agentica de modelos. El modelador propuso Isolation Forest, PCA con error de
reconstruccion y una variante conservadora de Isolation Forest:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_cwru_agentic_model_comparison \
  --model qwen3.5:4b \
  --plan-id cwru-agentic-model-qwen-fase3
\end{verbatim}
\end{minipage}
\end{center}

\begin{table}[h]
\centering
\caption{Comparacion agentica de modelos sobre CWRU}
\label{tab:cwru-agentic-model}
\begin{tabular}{lccccc}
\toprule
Modelo & Umbral & Precision & Recall & F1 & FPR \\
\midrule
Isolation Forest & 0.99 & 0.9996 & 0.9999 & 0.9998 & 0.0212 \\
PCA reconstruction & 0.99 & 1.0000 & 0.9623 & 0.9808 & 0.0000 \\
Isolation Forest cons. & 1.00 & 0.9999 & 0.9994 & 0.9996 & 0.0085 \\
\bottomrule
\end{tabular}
\end{table}

PCA elimina falsos positivos en este protocolo, pero reduce el recall a
0.9623. Esta comparacion es relevante porque rompe el patron de resultados
perfectos y muestra un trade-off mas creible entre sensibilidad y falsas
alarmas.

\section{Prueba Smoke NASA IMS con Agentes Qwen}

Dentro de la Fase 3 se ha ejecutado una prueba smoke sobre una estructura NASA
IMS sintetica preextraida. La ejecucion usa el grafo completo y agentes
Qwen/Ollama, pero bloquea deliberadamente el modelado final porque el dataset
run-to-failure aun no dispone de etiquetas por ventana ni de una politica de
particion temporal validada.

\begin{center}
\begin{minipage}{0.85\textwidth}
\begin{verbatim}
python -m codigo.scripts.run_nasa_ims_qwen_smoke \
  --model qwen3.5:4b \
  --run-id nasa-ims-qwen-smoke-fase3-qwen
\end{verbatim}
\end{minipage}
\end{center}

El snapshot queda persistido en:

\begin{center}
\begin{minipage}{0.85\textwidth}
\begin{verbatim}
codigo/reports/runs/nasa-ims-qwen-smoke-fase3-qwen/
\end{verbatim}
\end{minipage}
\end{center}

La prueba registro 12 decisiones agenticas. Entre ellas, el limpiador selecciono
`channel\_1`, el estructurador eligio ventanas de 1024 muestras con solape del
50\% y el modelador propuso `isolation\_forest`. El resultado final fue
`failed`, no por fallo de ingestion o procesamiento, sino por guardarrail
metodologico. Por tanto, esta ejecucion se interpreta como validacion de la
adaptacion inicial de agentes y artefactos, no como experimento de rendimiento
NASA IMS.

Para evitar presentar metricas supervisadas sin etiquetas defendibles, se ha
anadido un diagnostico no supervisado de degradacion sobre las features NASA
generadas:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_nasa_ims_degradation_diagnostics
\end{verbatim}
\end{minipage}
\end{center}

El diagnostico no calcula recall ni F1. Sobre la prueba sintetica NASA IMS
detecta una tendencia creciente: media inicial 0.3080, media final 0.6866,
ratio final/inicial 2.2296 y pendiente temporal 0.1893.

\section{Benchmark NASA IMS Sintetico Etiquetado}

Para obtener una evaluacion supervisada multi-dataset sin usar etiquetas
inventadas sobre NASA IMS real, se ejecuto un benchmark sintetico temporal. La
estructura mantiene el formato NASA IMS Set 2, pero las etiquetas binarias son
controladas por construccion.

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_nasa_ims_synthetic_temporal_benchmark \
  --model qwen3.5:4b \
  --run-id nasa-ims-synth-agentic-qwen-fase3
\end{verbatim}
\end{minipage}
\end{center}

\begin{table}[h]
\centering
\caption{Run agentica sobre benchmark NASA IMS sintetico etiquetado}
\label{tab:nasa-ims-synth-agentic}
\begin{tabular}{lccccc}
\toprule
Modelo & Precision & Recall & F1 & ROC-AUC & FPR \\
\midrule
Isolation Forest & 0.9130 & 0.6000 & 0.7241 & 0.8041 & 0.1429 \\
\bottomrule
\end{tabular}
\end{table}

La ejecucion quedo rechazada por el agente evaluador, ya que no cumple el
recall minimo de 0.90 ni la tasa maxima de falsos positivos de 0.10. Este
resultado es deliberadamente util: aporta un caso no perfecto sobre un dataset
distinto de CWRU y muestra que el sistema no solo informa exitos, sino tambien
fallos tecnicos que requieren una nueva iteracion de configuracion.

\section{Iteracion Agentica sobre NASA IMS Sintetico}

A partir de la run rechazada se ejecuto un bucle de reintento acotado, con dos
intentos maximos. El modelador recibio un analisis de fallo con falsos
negativos, falsos positivos y convencion del umbral. En ambos intentos Qwen
interpreto que el problema principal era la perdida de anomalias y propuso
bajar `threshold\_quantile`.

\begin{table}[h]
\centering
\caption{Reintentos agenticos sobre benchmark NASA IMS sintetico}
\label{tab:nasa-ims-agentic-retry}
\begin{tabular}{lccccc}
\toprule
Run & Umbral & Precision & Recall & F1 & FPR \\
\midrule
Base & 0.99 & 0.9130 & 0.6000 & 0.7241 & 0.1429 \\
Retry 1 & 0.95 & 0.8846 & 0.6571 & 0.7541 & 0.2143 \\
Retry 2 & 0.50 & 0.7143 & 1.0000 & 0.8333 & 1.0000 \\
\bottomrule
\end{tabular}
\end{table}

El resultado ilustra el comportamiento buscado para el TFM. El agente aprende
de los falsos negativos y actua en la direccion tecnica correcta: reducir el
umbral aumenta el recall. Sin embargo, el segundo reintento muestra tambien el
riesgo industrial de optimizar solo sensibilidad: se alcanza recall 1.0000,
pero todos los normales del split de test pasan a clasificarse como anomalias.
La ejecucion queda rechazada por FPR excesiva y el sistema detiene el bucle con
`next\_action = stop` al agotarse el presupuesto de reintentos. Por tanto, la
arquitectura permite deliberacion y correccion agentica, pero conserva un
limite explicito contra busquedas indefinidas o aprobaciones engañosas.

Sobre estos reintentos se genero una auditoria de razonamiento. El primer caso
se clasifico automaticamente como `partially\_supported`, ya que la hipotesis
mejoro recall y F1 pero mantuvo FPR por encima del umbral. El segundo se
clasifico como `overcorrected`: la direccion del razonamiento era correcta,
pero la magnitud del cambio fue insegura. En ambos casos se creo una solicitud
de revision humana para que el razonamiento pueda etiquetarse posteriormente
como ejemplo positivo, negativo o caso frontera antes de incorporarlo a una
memoria contextual.

\section{Primer Experimento con Memoria Agentica}

Como primer experimento de Fase 4 se activo la memoria RAG del modelador sobre
la misma run NASA sintetica rechazada. Previamente se habia incorporado a
`modeler\_memory` el segundo reintento de Fase 3 como `boundary\_case`
revisado: la direccion de bajar el umbral era correcta, pero la magnitud habia
producido FPR 1.0000.

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_nasa_ims_agentic_retry \
  --model qwen3.5:4b \
  --source-run-id nasa-ims-synth-agentic-qwen-fase3 \
  --run-id-prefix nasa-ims-synth-agentic-qwen-fase4-memory \
  --max-attempts 1 \
  --use-memory \
  --memory-dir codigo/reports/reasoning_memory
\end{verbatim}
\end{minipage}
\end{center}

El sistema recupero el recuerdo correcto y el agente declaro haberlo usado. La
decision cito explicitamente el `memory\_record\_id` del caso de
sobrecorreccion. Sin embargo, Qwen volvio a proponer
`threshold\_quantile=0.50`, reproduciendo la misma sobrecorreccion que la
memoria advertia.

\begin{table}[h]
\centering
\caption{Reintento con memoria agentica sobre NASA IMS sintetico}
\label{tab:nasa-ims-agentic-memory}
\begin{tabular}{lccccc}
\toprule
Run & Umbral & Precision & Recall & F1 & FPR \\
\midrule
Base & 0.99 & 0.9130 & 0.6000 & 0.7241 & 0.1429 \\
Memoria & 0.50 & 0.7143 & 1.0000 & 0.8333 & 1.0000 \\
\bottomrule
\end{tabular}
\end{table}

Este resultado es importante porque separa dos capacidades distintas. La
infraestructura de memoria funciona: recupera el caso relevante, lo inyecta en
el prompt y exige que el agente cite los recuerdos usados. No obstante, el LLM
local no siempre convierte esa evidencia en una decision prudente. Para la
siguiente iteracion sera necesario auditar el uso de memoria: el agente debera
explicar como evita repetir fallos asociados a recuerdos `boundary\_case` o
`warning`, no solo declarar que los ha leido.

Tras endurecer el contrato, se ejecuto una segunda variante con auditoria de
uso de memoria:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_nasa_ims_agentic_retry \
  --model qwen3.5:4b \
  --source-run-id nasa-ims-synth-agentic-qwen-fase3 \
  --run-id-prefix nasa-ims-synth-agentic-qwen-fase4-memory-audit \
  --max-attempts 1 \
  --use-memory \
  --memory-dir codigo/reports/reasoning_memory
\end{verbatim}
\end{minipage}
\end{center}

En esta segunda ejecucion, Qwen no solo cito la memoria, sino que declaro que la
usaba como `adapted`, explico su influencia y propuso una mitigacion del riesgo
de sobrecorreccion. Aun asi, volvio a elegir `threshold\_quantile=0.50` y el
resultado mantuvo FPR 1.0000. La nueva auditoria clasifico el caso como
repeticion de fallo de frontera, proporcionando evidencia estructurada de que
el agente habia leido la advertencia pero no la habia transformado en una accion
suficientemente distinta.

La auditoria se incorporo despues al indice RAG como una nueva memoria de tipo
`warning`, usando `memory\_usage\_audit` como fuente. Al reconstruir la memoria
con `qwen3-embedding:0.6b`, el indice del modelador contiene dos recuerdos:
el caso frontera revisado por humano y la advertencia derivada de la auditoria.
En una consulta de humo sobre repeticion de fallo por umbral, la advertencia se
recupera antes que el caso frontera, con uso `negative\_warning`. Esto prepara
la siguiente ejecucion: evaluar si el agente cambia su decision cuando no solo
recibe el recuerdo original, sino tambien la evidencia de que ya lo interpreto
mal una vez.

La ejecucion con memoria ampliada produjo un cambio cualitativo. Qwen recupero
y cito los dos recuerdos, pero esta vez no repitio `threshold\_quantile=0.50`.
Propuso un ajuste conservador a `threshold\_quantile=0.95`, explicando que la
direccion de bajar el umbral seguia siendo razonable pero que la magnitud debia
limitarse por la advertencia de sobrecorreccion. El resultado mejoro recall
`0.6000 -> 0.6571` y F1 `0.7241 -> 0.7541`, pero aumento FPR
`0.1429 -> 0.2143`. Por ello, el evaluador rechazo la ejecucion. La auditoria
de memoria clasifico el uso como `memory\_aligned`: el agente aplico la leccion
de no repetir el salto radical, aunque el ajuste resultante siguio siendo
insuficiente.

Tras conectar los post-mortems con el contrato `DecisionEpisode`, se ejecuto
una nueva run Qwen con el mismo protocolo. El resultado numerico fue equivalente
al reintento anterior, pero la ejecucion genero automaticamente
`decision\_episode.json` y `memory\_candidate.json`. El candidato quedo
clasificado como `boundary\_case` para el modelador: documenta que la memoria
evito repetir el cambio radical de umbral, pero que el ajuste moderado solo
produjo una mejora parcial y aumento FPR. Al reconstruir la memoria canonica
con embeddings Qwen, este candidato entro en `modeler\_memory` como recuerdo
de tipo `decision\_episode`.

Una consulta de humo sobre mejora parcial de umbral y comparacion de familias
de modelo recupero el nuevo episodio como `boundary\_context`, junto con el
caso humano revisado y las auditorias de uso de memoria. Esto confirma que la
memoria ya contiene episodios completos de decision, no solo advertencias
puntuales sobre umbrales.

Con esa memoria por episodios se ejecuto una nueva variante orientada a
comprobar si el modelador dejaba de razonar exclusivamente sobre el umbral. El
contrato de prompt incluyo `source\_type` y candidatos comparables. La run
`nasa-ims-synth-agentic-qwen-fase4-model-family-attempt-01` recupero cinco
recuerdos, cito tres de ellos y anadio explicitamente
`pca\_reconstruction\_error\_candidate` como alternativa. La configuracion
ejecutada siguio siendo Isolation Forest con umbral 0.95, por lo que las
metricas fueron equivalentes al caso anterior: precision 0.8846, recall 0.6571,
F1 0.7541 y FPR 0.2143. El avance principal no fue metrico, sino de
trazabilidad: el agente dejo evidencia de una comparacion entre familias de
modelo dentro del contrato validado.

La misma ejecucion revelo una limitacion nueva. Aunque Qwen uso memoria y
planteo PCA como alternativa, su explicacion incluyo una inconsistencia
numerica: describio `0.95` como mayor que `0.99` y trato el cuantil `0.99` como
si fuera bajo. Para convertir este fallo en evidencia reutilizable se amplio la
auditoria de memoria y se ejecuto
`nasa-ims-synth-agentic-qwen-fase4-coherence-audit-attempt-01`. La auditoria
clasifico el resultado como `memory\_reasoning\_inconsistent` y guardo el
motivo concreto en `memory\_usage\_audit.json`. Tras reconstruir el indice con
`qwen3-embedding:0.6b`, `modeler\_memory` contiene 9 registros: un caso humano
revisado, cinco auditorias de uso y tres episodios de decision.

El ultimo ajuste de esta serie consistio en convertir la recomendacion de
cambiar de familia en una guia ejecutable dentro del prompt. Cuando la memoria
indica que los ajustes de umbral en Isolation Forest ya han producido mejoras
parciales rechazadas, la guia de reintento marca PCA como configuracion
preferente o, alternativamente, permite que el agente decida parar. Con esta
guia, la run final de PCA ejecuto realmente el modelo por error de
reconstruccion con umbral 0.99.

\begin{table}[h]
\centering
\caption{Cambio de familia guiado por memoria sobre NASA IMS sintetico}
\label{tab:nasa-ims-agentic-pca-memory}
\begin{tabular}{lccccc}
\toprule
Run & Modelo & Precision & Recall & F1 & FPR \\
\midrule
Base & IForest 0.99 & 0.9130 & 0.6000 & 0.7241 & 0.1429 \\
IForest memoria & IForest 0.95 & 0.8846 & 0.6571 & 0.7541 & 0.2143 \\
PCA memoria & PCA 0.99 & 0.9375 & 0.4286 & 0.5882 & 0.0714 \\
\bottomrule
\end{tabular}
\end{table}

El resultado muestra un trade-off claro: PCA redujo la tasa de falsos positivos
por debajo del umbral operativo, pero perdio demasiadas anomalias, con recall
0.4286. La ejecucion fue rechazada por el evaluador, pero la memoria logro su
objetivo metodologico: provocar un cambio real de familia de modelo y generar
evidencia sobre por que esa alternativa concreta tampoco es suficiente en su
configuracion conservadora.

\section{Memoria Transversal en una Run Completa CWRU}

Despues de validar la memoria del modelador, se ejecuto el pipeline CWRU
completo con memoria transversal activada para estructurador y evaluador. La
primera run uso agentes deterministas y sirvio para generar los primeros
episodios reutilizables de esos dos roles:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_cwru_pipeline_with_memory \
  --run-id cwru-memory-transversal-fase4-001 \
  --rebuild-index \
  --clear-existing \
  --embedding-provider ollama \
  --embedding-model qwen3-embedding:0.6b
\end{verbatim}
\end{minipage}
\end{center}

La run fue aprobada con precision 0.9991, recall 1.0000, F1 0.9996 y FPR
0.0513. Tras reconstruir el indice, aparecieron por primera vez las memorias
del estructurador y del evaluador, con un registro cada una.

La segunda run activo Qwen para comprobar si esos recuerdos eran recuperados y
citados por agentes reales:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_cwru_pipeline_with_memory \
  --run-id cwru-memory-transversal-fase4-002-llm \
  --use-llm \
  --model qwen3.5:4b \
  --rebuild-index \
  --clear-existing \
  --embedding-provider ollama \
  --embedding-model qwen3-embedding:0.6b
\end{verbatim}
\end{minipage}
\end{center}

El estructurador declaro `used\_memory\_context=true` y cito el episodio de
estructuracion de la run anterior. El evaluador hizo lo mismo con el episodio
de evaluacion previo. Las metricas finales fueron equivalentes a la primera
run y la ejecucion quedo aprobada. El indice reconstruido contiene veinte
registros: dieciseis en `modeler\_memory`, dos en `structurer\_memory` y dos en
`evaluator\_memory`.

La importancia de este experimento es cualitativa. La memoria ya no solo ayuda
a un reintento de modelado, sino que se propaga por el proceso completo:
estructura, evaluacion y modelado pueden dejar y recuperar experiencias
separadas por rol. Al mismo tiempo, el evaluador sigue aprobando solo por las
metricas actuales, no porque exista un recuerdo recuperado.

Como cierre del hito, se ejecuto la auditoria cualitativa transversal:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.audit_transversal_memory_run \
  --run-id cwru-memory-transversal-fase4-002-llm
\end{verbatim}
\end{minipage}
\end{center}

La auditoria clasifico el uso de memoria como `memory\_aligned` para el
estructurador y para el evaluador. Ambos agentes citaron recuerdos que habian
sido recuperados en su contexto RAG y declararon el uso como `adapted`. En el
caso del evaluador, la aprobacion queda respaldada por las metricas actuales
del run, no por la existencia de memoria previa: recall 1.0000 frente al umbral
0.9000 y FPR 0.0513 frente al maximo 0.1000. La auditoria recomienda revision
humana si estos candidatos se quieren tratar como evidencia principal, aunque
permite su reutilizacion como contexto trazable.

\section{Validacion del Runner Comun Multi-dataset}

Para comprobar que la capa comun de planificacion no modifica el comportamiento
ya validado, se ejecuto CWRU mediante el runner multi-dataset:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_dataset_pipeline_with_memory \
  --dataset-id cwru_bearing \
  --adapter-id cwru_bearing \
  --raw-path codigo/data/raw/cwru_bearing/mat \
  --run-id cwru-runner-common-validation-fase4-001
\end{verbatim}
\end{minipage}
\end{center}

La ejecucion completo el pipeline, fue aprobada y no produjo errores. Las
metricas fueron precision 0.9991497803599263, recall 1.0000, F1
0.9995747093847462 y FPR 0.05128205128205128. Estos valores coinciden
exactamente con las dos referencias CWRU previas usadas como baseline, por lo
que el runner comun no introduce regresion en el benchmark principal.

Tambien se ejecuto NASA IMS en modo diagnostico:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_dataset_pipeline_with_memory \
  --dataset-id nasa_ims_bearing \
  --adapter-id nasa_ims_bearing \
  --raw-path codigo/data/raw/nasa_ims_bearing/preextracted_synthetic_qwen \
  --run-id nasa-runner-common-diagnostic-fase4-001 \
  --execution-mode diagnostic
\end{verbatim}
\end{minipage}
\end{center}

La ejecucion termino sin errores, pero sin metricas ni evaluacion. Solo se
ejecutaron manifiesto, perfilado, limpieza y estructuracion; los artefactos
generados fueron manifiesto, perfil, senales limpias, log, features, tensores
y splits. Esto confirma que el modo diagnostico no entra en modelado
supervisado.

Por ultimo, se comprobo que NASA IMS en modo completo sigue bloqueado en la
fase de planificacion, con `can\_execute\_requested\_stages=false` y bloqueos en
`modeling` y `evaluation`. La causa declarada es la ausencia de una politica
temporal y de etiquetas por ventana defendible para NASA IMS real.

Una vez implementada la primera politica temporal versionada, se repitio la
planificacion y ejecucion completa declarando:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_dataset_pipeline_with_memory \
  --dataset-id nasa_ims_bearing \
  --adapter-id nasa_ims_bearing \
  --raw-path codigo/data/raw/nasa_ims_bearing/preextracted_synthetic_qwen \
  --run-id nasa-runner-temporal-policy-v1-completed-low-metrics-002 \
  --dataset-policy-id nasa_ims_temporal_v1
\end{verbatim}
\end{minipage}
\end{center}

Con la politica temporal v1, el runner genero un manifiesto derivado, etiqueto
dos snapshots como `normal` y uno como `degradation`, y la estructuracion uso
los hints de split declarados en `metadata\_json`: un snapshot normal para entrenamiento, un
snapshot normal para test y un snapshot de degradacion para test. La ejecucion
alcanzo modelado y evaluacion sin errores de infraestructura. Sobre el split de
test se obtuvieron precision 0.5000, recall 1.0000, F1 0.6667 y FPR 1.0000.
El evaluador marco la run como `approved=false` por exceso de falsos positivos,
pero el supervisor la completo y genero informe final. La conclusion
experimental es que la politica temporal desbloquea una ejecucion completa y
auditable, aunque las metricas siguen siendo proxy metodologicas y requieren un
protocolo mas robusto antes de usarse como resultado principal.

\section{Experimento de cierre de Fase 9}

El cierre de Fase 9 se valida con dos tipos de prueba: una suite comparable de
modelos run-to-failure y una run agentica libre con Qwen. La primera comprueba
que las familias PCA, Isolation Forest, One-Class SVM y autoencoder PyTorch se
ejecutan bajo el mismo protocolo temporal. La segunda comprueba que el agente
\texttt{modeler} mantiene capacidad de decision y no queda sustituido por una
regla que fuerce el autoencoder.

La suite ligera se ejecuto sobre
\texttt{synthetic\_autoencoder\_readiness\_lite}, un conjunto NASA IMS-like de
24 snapshots sinteticos preparado para no convertir el cierre de fase en un
benchmark nuevo. La herramienta de readiness declaro
\texttt{autoencoder\_ready=true}, \texttt{readiness\_level=caution} y
\texttt{n\_train\_nominal\_windows=35}. RUL se mantuvo bloqueado por existir
una sola trayectoria completa, lo que valida que readiness permite
autoencoder pero no abre automaticamente pronostico de vida util.

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_run_to_failure_model_suite \
  --plan-id fase9-hito4b-ae-readiness-lite-001 \
  --raw-path codigo/data/raw/nasa_ims_bearing/synthetic_autoencoder_readiness_lite
\end{verbatim}
\end{minipage}
\end{center}

La segunda prueba ejecuto el pipeline comun con agentes Qwen/Ollama reales:

\begin{center}
\begin{minipage}{0.95\textwidth}
\footnotesize
\begin{verbatim}
python -m codigo.scripts.run_dataset_pipeline_with_memory \
  --dataset-id nasa_ims_bearing \
  --adapter-id nasa_ims_bearing \
  --dataset-policy-id nasa_ims_temporal_v1 \
  --run-id fase9-agentic-modeler-readiness-lite-llm-guardrail-001 \
  --raw-path codigo/data/raw/nasa_ims_bearing/synthetic_autoencoder_readiness_lite \
  --use-llm
\end{verbatim}
\end{minipage}
\end{center}

Esta run finalizo como \texttt{completed} y \texttt{approved=true}. El
\texttt{modeler} eligio \texttt{pca\_reconstruction\_error}, no el
autoencoder, priorizando interpretabilidad temporal y robustez ante etiquetas
proxy. Al mismo tiempo, mantuvo \texttt{autoencoder\_dense\_temporal} como
candidato comparable condicionado a readiness suficiente. Este resultado es
metodologicamente importante: el autoencoder existe y puede ejecutarse, pero no
quita protagonismo al agente.

Por ultimo, se valido el hardening de fallbacks visibles. La misma linea de
experimentos mostro que \texttt{supervisor}, \texttt{evaluator} y
\texttt{report\_writer} inicial responden con LLM real y confianza alta; las
salidas de contrato incorrectas se registran como \texttt{Guardrail
correction} y no como fallos opacos de LLM. La verificacion completa de codigo
tras el cierre produjo:

\begin{center}
\begin{minipage}{0.85\textwidth}
\footnotesize
\begin{verbatim}
python -m unittest discover codigo/tests
# Ran 352 tests
# OK (skipped=1)
\end{verbatim}
\end{minipage}
\end{center}

\section{Validacion operativa de la aplicacion local}

La Fase 5 no introduce un nuevo benchmark numerico, sino una validacion
operativa de la aplicacion que envuelve el pipeline. El criterio experimental
pasa a ser comprobar que el flujo completo puede ejecutarse desde una interfaz
web local sin duplicar la logica de planificacion ni leer artefactos
directamente desde el navegador.

La validacion se organizo alrededor de los siguientes puntos:

\begin{itemize}
  \item compilacion del frontend React/Vite mediante `npm run build`;
  \item comprobacion de `GET /health` a traves del proxy local del frontend;
  \item comprobacion de `GET /llm/status` para verificar disponibilidad de
        Ollama y del modelo `qwen3.5:4b`;
  \item preflight de CWRU mediante `POST /runs` con `dry\_run=true`;
  \item ejecucion en segundo plano con polling de job local;
  \item consulta de snapshot, metricas, informe, artefactos, memoria,
        observabilidad agentica y visualizacion;
  \item preflight de NASA IMS conservando los bloqueos o advertencias
        metodologicas declaradas por la politica de dataset.
\end{itemize}

Esta validacion no sustituye las metricas de los experimentos anteriores. Su
valor es demostrar que la arquitectura completa puede operarse como una
aplicacion local, manteniendo FastAPI como frontera canonica, los snapshots
persistidos como fuente de verdad y los ejecutores deterministas como unica
capa que transforma datos.

\section{Experimentos posteriores}

Experimentos previstos:

\begin{itemize}
  \item validacion de la misma metodologia sobre un segundo dataset
        run-to-failure real, preferentemente XJTU-SY o PRONOSTIA;
  \item extension del RUL experimental con intervalos e incertidumbre, sin
        presentarlo como prediccion industrial validada;
  \item comparacion entre features temporales, frecuenciales y representaciones
        aprendidas cuando existan suficientes trayectorias completas;
  \item validacion leave-one-run-out o por condicion operativa para reducir
        riesgo de fuga temporal;
  \item prueba Docker completa tras incorporar PyTorch como dependencia del
        entorno local.
\end{itemize}
