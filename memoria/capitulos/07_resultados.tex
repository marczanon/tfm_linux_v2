\chapter{Resultados}

\section{Resultados cuantitativos}

La validacion cuantitativa se ha organizado en dos niveles. Primero se conserva
el benchmark CWRU como referencia de regresion del MVP. Despues se incorporan
experimentos de Fase 3 donde los agentes locales proponen alternativas de
ventana, modelo y reintento sobre evidencia persistida.

\subsection{Baseline CWRU de Fase 2}

La Fase 2 incorpora una comparacion local entre dos ejecuciones persistidas del
baseline Isolation Forest sobre CWRU. Ambas ejecuciones mantienen constante el
dataset, la limpieza, la estructuracion temporal, la semilla y
`n\_estimators=200`; la unica variable modificada es el cuantil usado para
fijar el umbral de anomalia.

\begin{table}[htbp]
\centering
\begin{tabular}{lrrrrr}
\toprule
Experimento & Umbral & Precision & Recall & F1 & FPR \\
\midrule
Baseline & 0.99 & 0.9991 & 1.0000 & 0.9996 & 0.0513 \\
Conservador & 1.00 & 0.9996 & 1.0000 & 0.9998 & 0.0256 \\
\bottomrule
\end{tabular}
\caption{Comparacion de umbral con Isolation Forest en el split de test CWRU.}
\label{tab:resultados_modelos_fase2}
\end{table}

La variante conservadora reduce los falsos positivos sin penalizar el recall.
En el baseline, la matriz de confusion del split de test contiene 111
verdaderos negativos, 6 falsos positivos, 0 falsos negativos y 7051 verdaderos
positivos. En la variante con umbral 1.00, los falsos positivos bajan a 3, con
114 verdaderos negativos, 0 falsos negativos y 7051 verdaderos positivos.

\subsection{Comparacion agentica de ventanas}

En Fase 3 el agente estructurador Qwen no recibe una unica configuracion fija,
sino que puede proponer alternativas comparables. Sobre CWRU propuso tres
ventanas: 2048 muestras como opcion principal, 1024 muestras para mayor
resolucion temporal y 4096 muestras para mayor contexto por ventana. El
protocolo experimental materializo solo las alternativas propuestas por el
agente y persistio cada run.

\begin{table}[htbp]
\centering
\begin{tabular}{rrrrr}
\toprule
Ventana & Precision & Recall & F1 & FPR \\
\midrule
1024 & 0.9996 & 0.9999 & 0.9998 & 0.0212 \\
2048 & 0.9991 & 1.0000 & 0.9996 & 0.0513 \\
4096 & 0.9991 & 1.0000 & 0.9996 & 0.0517 \\
\bottomrule
\end{tabular}
\caption{Comparacion agentica de tamanos de ventana en CWRU con 50\% de solape.}
\label{tab:resultados_ventanas_fase3}
\end{table}

La alternativa de 1024 muestras obtuvo el mejor F1 y la menor tasa de falsos
positivos. Este resultado es relevante porque la mejora no procede de una
busqueda exhaustiva codificada, sino de una hipotesis propuesta por el agente y
despues evaluada por ejecutores deterministas.

\subsection{Comparacion agentica de modelos}

La Fase 3 tambien amplio el espacio de decision del modelador con PCA basado en
error de reconstruccion. Qwen propuso comparar Isolation Forest, PCA y una
variante conservadora de Isolation Forest usando la ventana de 1024 muestras
seleccionada en el experimento anterior.

\begin{table}[htbp]
\centering
\begin{tabular}{lrrrrr}
\toprule
Modelo & Umbral & Precision & Recall & F1 & FPR \\
\midrule
Isolation Forest & 0.99 & 0.9996 & 0.9999 & 0.9998 & 0.0212 \\
PCA reconstruction & 0.99 & 1.0000 & 0.9623 & 0.9808 & 0.0000 \\
Isolation Forest conservador & 1.00 & 0.9999 & 0.9994 & 0.9996 & 0.0085 \\
\bottomrule
\end{tabular}
\caption{Comparacion agentica de modelos sobre CWRU con ventana de 1024 muestras.}
\label{tab:resultados_modelos_fase3}
\end{table}

PCA ofrece un intercambio distinto: elimina falsos positivos en este protocolo,
pero reduce el recall. Este resultado evita que toda la evidencia de la fase
dependa de metricas perfectas y muestra que el agente puede comparar
alternativas con ventajas y limitaciones tecnicas diferentes.

\subsection{Benchmark NASA sintetico y reintentos}

NASA IMS real se mantiene bloqueado para metricas supervisadas hasta definir una
politica temporal y de etiquetado defendible. Para obtener un caso no trivial
sin atribuir etiquetas artificiales al dataset real, se genero un benchmark
sintetico con forma de entrada NASA IMS y etiquetas conocidas por construccion.

La primera ejecucion agentica obtuvo precision 0.9130, recall 0.6000, F1
0.7241, ROC-AUC 0.8041, PR-AUC 0.8842 y FPR 0.1429. El evaluador rechazo la run
al detectar que las metricas no cumplian los umbrales locales. Despues se activo
un bucle de reintento acotado donde el modelador recibia analisis de error y
decidia si modificar la configuracion o detenerse.

\begin{table}[htbp]
\centering
\begin{tabular}{lrrrrr}
\toprule
Run & Umbral & Precision & Recall & F1 & FPR \\
\midrule
Base & 0.99 & 0.9130 & 0.6000 & 0.7241 & 0.1429 \\
Reintento 1 & 0.95 & 0.8846 & 0.6571 & 0.7541 & 0.2143 \\
Reintento 2 & 0.50 & 0.7143 & 1.0000 & 0.8333 & 1.0000 \\
\bottomrule
\end{tabular}
\caption{Reintentos agenticos sobre benchmark NASA sintetico etiquetado.}
\label{tab:resultados_nasa_sintetico_retry}
\end{table}

El primer reintento mejora recall y F1, aunque aumenta la tasa de falsos
positivos. El segundo reintento recupera todas las anomalias, pero clasifica
tambien todos los normales de test como anomalos. El evaluador rechaza esta
sobrecorreccion y el bucle se detiene por limite de intentos. El resultado es
valioso porque muestra aprendizaje local de errores, pero tambien control ante
una mejora aparente que seria inaceptable operativamente.

\subsection{Primer resultado con memoria agentica}

En Fase 4 se realizo una primera prueba con memoria vectorial activa. El
modelador recupero un recuerdo revisado del segundo reintento de Fase 3,
etiquetado como `boundary\_case` por sobrecorreccion del umbral. El agente
declaro que habia usado memoria y cito el identificador del recuerdo
recuperado. Por tanto, la trazabilidad del RAG funciono correctamente.

\begin{table}[htbp]
\centering
\begin{tabular}{lrrrrr}
\toprule
Run & Umbral & Precision & Recall & F1 & FPR \\
\midrule
Base sin memoria & 0.99 & 0.9130 & 0.6000 & 0.7241 & 0.1429 \\
Retry con memoria & 0.50 & 0.7143 & 1.0000 & 0.8333 & 1.0000 \\
\bottomrule
\end{tabular}
\caption{Primera comparacion con memoria agentica sobre NASA sintetico.}
\label{tab:resultados_memoria_fase4}
\end{table}

La conclusion es mixta. La recuperacion semantica funciona, pero el agente
interpreto el recuerdo de frontera como apoyo para repetir la accion que habia
generado la sobrecorreccion. El resultado demuestra una limitacion importante:
un RAG operativo no garantiza aprendizaje correcto si el contrato no exige
razonar sobre el tipo de recuerdo recuperado. Esta evidencia orienta el
siguiente ajuste de Fase 4: auditar si la memoria fue seguida, ignorada o
contradicha, especialmente en casos `boundary\_case` y `warning`.

Tras incorporar esta auditoria, se repitio la prueba. El agente declaro que
usaba el recuerdo como `adapted`, explico que la memoria advertia contra bajar
el umbral sin control y propuso una mitigacion. Sin embargo, la configuracion
final siguio siendo `threshold\_quantile=0.50` y las metricas repitieron FPR
1.0000. El nuevo artefacto de auditoria clasifico el resultado como repeticion
de fallo de frontera. Esto aporta una evidencia cualitativa mas fina: no solo
se sabe que el agente recupero memoria, sino tambien que su explicacion no fue
suficiente para evitar repetir el fallo.

El resultado se ha convertido a su vez en memoria reutilizable de tipo
`warning`. La consulta posterior al indice recupera primero esta advertencia y
despues el caso frontera original, lo que muestra que el sistema ya no solo
almacena decisiones post-mortem, sino tambien evidencia sobre como los agentes
usan o malinterpretan recuerdos previos. Este punto es relevante para la
defensa del TFM: la memoria agentica se comporta como un mecanismo de
retroalimentacion trazable, no como una lista estatica de ejemplos.

Al ejecutar de nuevo con esta memoria ampliada, el comportamiento cambio. El
modelador cito los dos recuerdos y propuso `threshold\_quantile=0.95` en lugar
de repetir `0.50`. Esta decision mejoro recall y F1 frente a la base, pero
tambien aumento FPR hasta 0.2143, por lo que la ejecucion siguio rechazada. El
resultado es positivo desde el punto de vista agentico, porque demuestra que la
memoria puede modificar la estrategia del LLM local, pero tambien muestra que
la mejora de razonamiento no implica automaticamente aprobar metricas. La
aplicacion conserva asi la separacion entre deliberacion generativa y
validacion experimental.

La ejecucion posterior con generacion automatica de episodios no cambio las
metricas, pero si cambio la calidad de la evidencia persistida. El sistema
produjo `decision\_episode.json` y `memory\_candidate.json`, y este ultimo se
incorporo a la memoria vectorial como `boundary\_case`. Una consulta semantica
sobre mejora parcial de umbral y necesidad de comparar familias de modelo
recupero este episodio junto a los recuerdos anteriores. Por tanto, el sistema
ya no solo aprende que un valor de umbral fue problematico, sino que conserva
una experiencia completa: contexto, accion, resultado, trade-off, rechazo del
evaluador y condiciones de reutilizacion.

La siguiente iteracion confirma esta interpretacion. Al recuperar el episodio
anterior, el modelador ya no recibio solo una advertencia sobre el umbral, sino
una memoria que sugeria comparar familia de modelo tras una mejora parcial. La
decision generada por Qwen incluyo `pca\_reconstruction\_error\_candidate` como
alternativa comparable, aunque la configuracion ejecutada siguio siendo
Isolation Forest con `threshold\_quantile=0.95`. El resultado numerico no
mejoro respecto a la run anterior, pero la evidencia cualitativa si: el sistema
puede registrar que el agente considero PCA y por que no lo selecciono.

Tambien se obtuvo una evidencia negativa valiosa. La auditoria detecto que el
razonamiento del agente contenia una comparacion numerica incoherente
(`0.95` mayor que `0.99`). El nuevo resultado
`memory\_reasoning\_inconsistent` muestra que el proyecto no solo mide metricas
del modelo, sino tambien la calidad de las explicaciones agenticas. Esta capa
es importante para un sistema con LLM locales: permite reutilizar fallos de
razonamiento como advertencias, sin convertirlos en ejemplos positivos.

Finalmente, se incorporo una guia derivada de memoria al prompt de reintento.
Cuando los recuerdos recuperados indicaban que los ajustes de umbral sobre
Isolation Forest ya habian producido mejoras parciales rechazadas, la guia
marcaba PCA por error de reconstruccion como configuracion preferente, sin
ejecutarla de forma determinista. Con esta condicion, Qwen eligio realmente
PCA como configuracion de reintento. El resultado fue precision 0.9375,
recall 0.4286, F1 0.5882 y FPR 0.0714. La ejecucion fue rechazada por bajo
recall, pero aporta una evidencia relevante: la memoria no solo cambia textos
o explicaciones del
agente, sino que puede desplazar la accion ejecutada hacia una nueva familia de
modelo. Al mismo tiempo, el evaluador conserva el control experimental y evita
aceptar una alternativa que reduce falsos positivos a costa de perder demasiadas
anomalias.

\subsection{Resultado con memoria transversal}

La extension de memoria a estructurador y evaluador se valido con dos runs CWRU
completas. La primera genero episodios y candidatos para ambos agentes; la
segunda, ejecutada con Qwen, recupero esos recuerdos. El estructurador y el
evaluador declararon `used\_memory\_context=true` y citaron los
`memory\_record\_id` correspondientes de la run anterior.

\begin{table}[htbp]
\centering
\begin{tabular}{lrrrrr}
\toprule
Run & Precision & Recall & F1 & FPR & Memorias \\
\midrule
Transversal 1 & 0.9991 & 1.0000 & 0.9996 & 0.0513 & 2 \\
Transversal 2 Qwen & 0.9991 & 1.0000 & 0.9996 & 0.0513 & 4 \\
\bottomrule
\end{tabular}
\caption{Primer ciclo de memoria transversal en CWRU.}
\label{tab:resultados_memoria_transversal}
\end{table}

El resultado no pretende demostrar mejora metrica, porque CWRU ya estaba casi
resuelto por el pipeline base. Su valor es arquitectonico: confirma que la
memoria agentica puede operar por rol dentro de una ejecucion completa. Tras la
segunda run, el indice contiene 20 registros: 16 en `modeler\_memory`, 2 en
`structurer\_memory` y 2 en `evaluator\_memory`. Esto demuestra que la memoria
supervisada empieza a representar aprendizaje de proceso, no solo ajustes de
hiperparametros.

La auditoria posterior del uso transversal clasifico ambas decisiones como
`memory\_aligned`. No se detectaron recuerdos citados fuera del contexto
recuperado ni declaraciones de uso ausentes. El evaluador mantuvo respaldo
metrico independiente de la memoria: recall 1.0000 y FPR 0.0513 bajo los
umbrales exigidos. Por tanto, los nuevos candidatos quedan aceptados como
contexto reutilizable, pero no como evidencia academica principal sin revision
humana.

\section{Resultados cualitativos}

La prueba smoke con agentes Qwen sobre una carpeta NASA IMS sintetica
preextraida confirma que el patron de expediente experto y decision
estructurada es viable con LLM locales. El limpiador selecciono un canal a
partir del perfil, el estructurador eligio una configuracion temporal soportada
y el modelador propuso un modelo ejecutable. Sin embargo, la run termino en
estado `failed` por guardarrail metodologico: NASA IMS no debe evaluarse como
problema supervisado hasta definir etiquetas por ventana y particion temporal
sin fuga.

En Fase 4 se ha dado el primer paso para retirar ese bloqueo de forma
controlada: la politica `nasa\_ims\_temporal\_v1` etiqueta el tramo inicial de
cada secuencia como normalidad proxy, el tramo posterior como degradacion proxy
y declara esos metadatos en el manifiesto derivado. La run
`nasa-runner-temporal-policy-v1-completed-low-metrics-002` alcanzo modelado,
evaluacion e informe final. Termino como `completed`, pero con
`approved=false` por FPR 1.0000. Este resultado es util precisamente porque
separa dos conceptos: la infraestructura ya puede ejecutar NASA IMS con
politica declarada, pero las metricas aun no son evidencia aprobada.

La Fase 3 tambien introduce auditoria del razonamiento. Cada reintento puede
generar un post-mortem que compara la hipotesis declarada por el agente con el
resultado real. En el benchmark NASA sintetico, el primer reintento queda como
`partially\_supported` y el segundo como `overcorrected`. Con el trigger de
revision humana se genera ademas una plantilla para que una persona etiquete si
el razonamiento debe reutilizarse, descartarse o conservarse como caso frontera.

Este punto refuerza la aportacion central del TFM: el sistema no solo registra
metricas finales, sino tambien por que un agente cambio una configuracion, que
efecto esperaba y si la evidencia posterior respalda ese razonamiento.

\subsection{Resultado operativo de Fase 5}

El resultado principal de Fase 5 es cualitativo y arquitectonico: el sistema
pasa de ser un conjunto de scripts, servicios y endpoints a una aplicacion web
local capaz de operar el pipeline completo. Desde la interfaz se puede
configurar una run, planificarla, detectar bloqueos metodologicos, ejecutarla
en segundo plano, seguir el job, abrir el snapshot, revisar metricas, informe y
artefactos, comparar ejecuciones, observar eventos agenticos, consultar memoria
persistida y visualizar una proyeccion PCA cuando existen artefactos
compatibles.

La activacion de `use\_llm=true` deja de ser un modo bloqueado y pasa a estar
condicionada por una comprobacion explicita de Ollama y del modelo
`qwen3.5:4b`. Esto confirma que la aplicacion web puede invocar agentes Qwen
reales sin relajar los contratos JSON/Pydantic ni permitir ejecucion de codigo
generado. Al mismo tiempo, la memoria se mantiene visible y consultable desde
la interfaz, pero su activacion como retrieval durante ejecuciones web queda
fuera del cierre de Fase 5 para no mezclar dos cambios metodologicos
independientes.

El rediseño final del frontend mejora tambien la interpretabilidad operacional:
se separan configuracion, preflight, ejecucion, historico, agentes, memoria y
visualizacion; se ocultan rutas locales de ficheros en la pagina web; y los
detalles tecnicos quedan disponibles sin dominar la lectura principal. Por
tanto, Fase 5 aporta una capa de producto local que hace defendible el sistema
ante una demo, aunque todavia no resuelve empaquetado, despliegue ni
multiusuario.

\section{Limitaciones observadas}

Los resultados sobre CWRU deben interpretarse como validacion del pipeline y del
protocolo experimental, no como garantia de generalizacion industrial. CWRU es
un benchmark controlado, con condiciones experimentales limpias y diferencias
claras entre normalidad y fallo.

NASA IMS real aporta mayor interes industrial. La politica temporal v1 permite
metricas supervisadas proxy, pero estas no deben presentarse como etiquetas
oficiales ni como resultado principal sin mas validacion. La version sintetica
permite estudiar el comportamiento multiagente ante metricas no perfectas, y la
politica temporal permite estudiar el flujo run-to-failure, pero ambos casos
requieren cautela metodologica.

Otra limitacion es que los agentes locales siguen dependiendo de la calidad del
expediente experto que reciben. El sistema evita delegar conocimiento de nicho
en la memoria parametrica del LLM, pero debe seguir ampliando diagnosticos,
modelos soportados y revisiones humanas para que la autonomia agentica sea
defendible en escenarios mas variados.
